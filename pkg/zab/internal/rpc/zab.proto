syntax = "proto3";

import "google/protobuf/empty.proto";

package zab;

option go_package = "pkg/zab/internal/rpc";

// The zab service definition.
service ZabService {
  // Ping RPC to check if the node is alive.
  rpc Ping(google.protobuf.Empty) returns (google.protobuf.Empty);
}
